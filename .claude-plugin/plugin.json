{
  "name": "specialized-agents",
  "version": "0.1.0",
  "description": "Grok Build plugin providing specialized agent types with unique mathematical and verification features. Flagship: sheaf-guardian - a subagent type that enforces topological/sheaf consistency (Laplacian energy, neural diffusion conditions) on semantic encodings and generated artifacts before output. Includes sheaf-coderabbit CLI for CodeRabbit-style local reviews focused on infil/exfil of truth at linkage points (hot restriction maps).",
  "author": {
    "name": "GrokBuild Workspace"
  },
  "license": "MIT",
  "keywords": ["agents", "sheaf", "topology", "verification", "spectral", "guard", "refactor", "synthesis"],
  "engines": {
    "grok": ">=0.2.0"
  },
  "mcpServers": {
    "sheaf-condition": {
      "type": "stdio",
      "command": "python",
      "args": ["${GROK_PLUGIN_ROOT}/scripts/mcp_server.py"],
      "env": {
        "GROK_PLUGIN_ROOT": "${GROK_PLUGIN_ROOT}",
        "PYTHONPATH": "${GROK_PLUGIN_ROOT}"
      }
    }
  }
}