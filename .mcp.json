{
  "sheaf-condition-mcp": {
    "type": "stdio",
    "command": "python",
    "args": [
      "${GROK_PLUGIN_ROOT}/scripts/mcp_server.py"
    ],
    "env": {
      "GROK_PLUGIN_ROOT": "${GROK_PLUGIN_ROOT}",
      "PYTHONPATH": "${GROK_PLUGIN_ROOT}"
    },
    "description": "Sheaf Condition MCP Server - exposes rotate_condition and read_condition_state tools for topological consistency gating using sheaf Laplacians and neural diffusion state. Used by the sheaf-guardian agent type for high-assurance synthesis."
  },
  "github": {
    "command": "npx",
    "args": [
      "-y",
      "@modelcontextprotocol/server-github"
    ],
    "env": {
      "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
    },
    "description": "GitHub MCP server for direct PR creation, branch management, issue tracking, and review comments. Critical for closing the 'no pr as stated' visibility gap and allowing the harness (meta-skill-creator, harness-backlog-reviewer) to autonomously surface task-yield reports and coderabbit findings as real PRs."
  }
}
